let nome = 'Joao paulo alves'
let valor = 'a'

let resultado = nome.split(' ')
console.log(resultado)

/*
if(nome.indexOf('J') > -1){
    console.log('Achou')
} else {
    console.log('Não achou')
}

for(let i = 0; i < nome.length; i++){
    if(valor.includes(nome[i])){
        console.log([i], nome[i])
    }
*/

// Filtragem:
// lenght, comprimento da expressão

// indexOf(), exibe posição numerica

// slice(posição, posição), busca posição inicial e final, -4 ele busca iniciando do final 

// substring()

// substr(0, quantidade), busta da posição e a quantidade de caracteres

// includes(), retorna valor boolean

// replace(x, y), troca um por outro

// toUpperCase()

// toLowerCase()

// concat() // concatena

// trim()

// split(''), transforma em um array usando as '' e passando o que será dividido

// charAt(parametro), busca a posição com o numero especificado, ou so []